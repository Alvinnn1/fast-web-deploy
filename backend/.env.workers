# Cloudflare Workers Environment Configuration
# This file contains environment-specific settings for Workers deployment
# Secrets should be set using: wrangler secret put <SECRET_NAME>

# API Configuration
NODE_ENV=production
API_VERSION=v1
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGINS=https://cloudflare-deployer.com,https://www.cloudflare-deployer.com

# Worker Configuration
WORKER_NAME=cloudflare-static-deployer-api
WORKER_ROUTE_PATTERN=api.cloudflare-deployer.com/*
WORKER_ZONE_NAME=cloudflare-deployer.com

# KV Namespace IDs (replace with actual IDs after creation)
CACHE_KV_NAMESPACE_ID=your_cache_kv_namespace_id
CACHE_KV_PREVIEW_ID=your_cache_kv_preview_id
SESSIONS_KV_NAMESPACE_ID=your_sessions_kv_namespace_id
SESSIONS_KV_PREVIEW_ID=your_sessions_kv_preview_id

# D1 Database IDs (replace with actual IDs after creation)
D1_DATABASE_NAME=cloudflare-deployer-db
D1_DATABASE_ID=your_d1_database_id

# R2 Bucket Names
R2_UPLOADS_BUCKET=cloudflare-deployer-uploads
R2_UPLOADS_PREVIEW_BUCKET=cloudflare-deployer-uploads-preview

# Secrets (set these using wrangler secret put)
# CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
# CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
# CLOUDFLARE_EMAIL=your_cloudflare_email

# Development Environment Overrides
# Uncomment and modify for development
# NODE_ENV=development
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# LOG_LEVEL=debug
# WORKER_NAME=cloudflare-static-deployer-api-dev

# Staging Environment Overrides
# Uncomment and modify for staging
# NODE_ENV=staging
# CORS_ORIGINS=https://staging.cloudflare-deployer.com
# WORKER_NAME=cloudflare-static-deployer-api-staging