# Backend Environment Variables Example
# Copy this file to backend/.env and update the values

# Node environment (development, staging, production)
# Required - should match the deployment environment
NODE_ENV=production

# Server port number
# Optional - defaults to 3000
PORT=3000

# Cloudflare API token for authentication
# Required - get this from Cloudflare dashboard
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here

# Allowed CORS origins (comma-separated)
# Required - should include your frontend domain
CORS_ORIGINS=https://yourdomain.com

# Logging level (debug, info, warn, error)
# Optional - defaults to info
LOG_LEVEL=warn