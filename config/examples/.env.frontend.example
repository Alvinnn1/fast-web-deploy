# Frontend Environment Variables Example
# Copy this file to frontend/.env.production and update the values

# Base URL for the backend API
# Required - must be a valid HTTP/HTTPS URL
VITE_API_BASE_URL=https://api.yourdomain.com

# Node environment (development, staging, production)
# Required - should match the deployment environment
VITE_NODE_ENV=production

# Enable debug mode (true/false)
# Optional - should be false in production
VITE_DEBUG=false

# Cloudflare account ID for API calls
# Optional - only needed if frontend makes direct Cloudflare API calls
VITE_CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id